# render.yaml — PERFECT FOR YOUR FASTAPI + VITE FOCUSFORGE APP
services:
  - type: web
    name: focusforge-api
    env: python
    region: oregon
    plan: free        # or starter/pro if you want more RAM
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      # Optional: force Gemini 2.5 Flash (fastest + cheapest)
      GEMINI_MODEL: models/gemini-2.5-flash
      # If you want Hindi voice later
      # GOOGLE_APPLICATION_CREDENTIALS: /etc/secrets/google.json